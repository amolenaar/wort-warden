
-- Config file
-- Copy or rename this file to `config.lua` and fill in the fields below.

return {
  ssid="your network",
  pwd="secret",
  token="Ubidits device token",

  -- I2C / gyroscope
  sda=3,
  scl=4,
  mpu6050_addr=0x68
}
